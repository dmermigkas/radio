package com.radio.Utilities;

public class Equals {

    @Override
    public boolean equals(Object object) {
        if (object == null) {
            return false;
        }
        if (this == object) {
            return true;
        }
        return true;
    }

    @Override
    public int hashCode() {
        int hash = 0;

        return hash;
    }

}
